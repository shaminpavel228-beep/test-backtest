# Contributing

Thanks for wanting to contribute! Please follow these guidelines:

- Create a branch with prefix `feature/` or `fix/`.
- Open a PR against `main` with a clear description of changes and reasoning.
- Add tests for functional changes where appropriate.
- Keep code style consistent and run `scripts/validate_pine.sh` before opening PR.
