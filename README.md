# test-backtest — TradingView Pine backtest project

Коротко: репозиторий с Pine Script (TradingView) для бэктестинга стратегий. Цель — привести скрипт к рабочему, хорошо документированному и тестируемому состоянию.

## Структура репозитория 🔧

- `pine/` — собранный Pine Script (`backtest.pine`) и исходники в `pine/src/` для модульной разработки. Файлы в `pine/src/` собираются в единый файл с помощью `scripts/build_pine.py`.
- `scripts/` — утилиты для проверки и форматирования скрипта (валидатор, авто-форматер).
- `.github/workflows/` — CI (валидация скрипта на push/pull_request).
- `.devcontainer/` — конфигурация для разработки в контейнере (удобно для агентов и людей).

---

## Быстрый старт 💡

1. Открой репозиторий в VS Code (рекомендую использование Dev Container).
2. Отредактируй `pine/backtest.pine` в редакторе (Pine синтаксис в файле — версия 5).
3. Запусти локальную валидацию: `./scripts/validate_pine.sh pine/backtest.pine`.

---

## Локальные подзадачи / Roadmap (TODO) ✅

- [ ] Провести ручной проход и проверить, что `backtest.pine` корректно загружается в TradingView (версия 5).
- [ ] Модульно разнести код: `indicators.pine`, `strategy.pine`, `dashboard.pine`.
- [ ] Добавить симулятор/юнит-тесты (Python) для проверки PnL/усреднений/ликвидации.
- [ ] Рефакторить логику управления капиталом (изолированная/кросс- маржа) и покрыть тестами.
- [ ] Добавить статический линтер и интеграцию с CI (GH Actions).
- [ ] Настроить шаблоны Issues/PR и CONTRIBUTING.md.
- [ ] Подготовить тестовый набор данных и примеры конфигураций для быстрого воспроизведения результатов.

Если хочешь, могу взять любую из этих задач в работу и сделать PR. Выбери приоритет — начнём с того, что важнее для тебя.

---

## Как вносить изменения

- Создавай ветки `feature/...` или `fix/...` и открывай PR в `main`.
- В PR добавляй описание изменений и тесты (если есть).

---

## Контакты и заметки

- Автор/контрибьютор: автоматический агент (GitHub Copilot)
- Модель: Raptor mini (Preview)

