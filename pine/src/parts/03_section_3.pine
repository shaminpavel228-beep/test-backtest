// ============================================
showDashboard = input.bool(true, "✅ Показать Дашборд", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
dashboardPosition = input.string("Top Right", "📍 Позиция дашборда", options=["Top Left", "Top Center", "Top Right", "Bottom Left", "Bottom Center", "Bottom Right"], group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
botMode = input.string("Оба", "🎯 Режим торговли", options=["Оба", "Только Лонг", "Только Шорт"], group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")

// НАСТРОЙКИ ВРЕМЕННОГО ПЕРИОДА
showTimePeriod = input.bool(true, "⏳ Временной период", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
useCustomStartDate = input.bool(true, "📅 Использовать начальную дату", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
startDate = input.time(timestamp("2024-08-05T00:00:00"), "📆 Начало торговли", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА", tooltip="Выберите начальную дату для бэктеста")
useRealTimeEnd = input.bool(true, "🔄 До реального времени", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА", tooltip="Если включено - бэктест до текущего момента")
endDate = input.time(timestamp("2026-01-18T00:00:00"), "📆 Конец торговли", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА", tooltip="Используется если 'До реального времени' выключено")

// Переключатели секций дашборда - ОРИГИНАЛЬНЫЙ ПОРЯДОК НАСТРОЕК
showCapitalSection = input.bool(true, "💰 Капитал", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showRiskSection = input.bool(true, "📊 Риск", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showStatusSection = input.bool(true, "⚡ Статус", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showSettingsSection = input.bool(true, "⚙️ Настройки", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showIndicatorsSection = input.bool(true, "📊 Индикаторы", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showPositionSection = input.bool(true, "📈 Позиция", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showStatisticsSection = input.bool(true, "📊 Статистика", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showProfitSection = input.bool(true, "💰 Прибыль", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showSignalsSection = input.bool(true, "🎯 Сигналы", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")
showCurrentSignalSection = input.bool(true, "🎯 Текущий сигнал", group="👁️ НАСТРОЙКИ И ОТОБРАЖЕНИЕ ДАШБОРДА")

